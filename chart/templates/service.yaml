apiVersion: v1
kind: Service
metadata:
  name: {{ include "vsh-docker-proj.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "vsh-docker-proj.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
